# $Id$

include @top_srcdir@/ligotools/ligotools.mk

EXTRA_DIST = changelog compat control control.in copyright libmetaio$(SONAME).install libmetaio-dev.install libmetaio-matlab.install libmetaio-utils.install Makefile.am Makefile.in postinst postrm rules watch source/format

clean-local :
	rm -Rf libmetaio-dev libmetaio-matlab libmetaio-utils libmetaio$(SONAME) files *debhelper *substvars
